# Story 3.8 — Workers: Listagem Dinâmica de Agentes na Elicitação

**Epic:** 3 — Governança e Versionamento
**Status:** Done
**Complexidade:** P
**Tipo:** kairos-core
**Criada por:** @kairos
**Data:** 2026-04-15

---

## Objetivo

A task `kairos-workers.md` hardcoda os nomes dos agentes desta instância (`@campaign-analyst / @lead-scorer / @niche-classifier / @email-writer`) na primeira pergunta da elicitação de `*workers new`. Isso é um acoplamento à instância que quebra o Kairos instalado do zero. Corrigir para leitura dinâmica de `core-config.yaml`.

---

## Critérios de Aceite

- [x] `kairos-workers.md` — Bloco 1, Pergunta 1 atualizada: em vez de listar agentes hardcoded, ler `core-config.yaml → agents.squads` e construir a lista dinamicamente no formato `@{agent-id} (squad: {squad-name})` para cada agente registrado
- [x] Se `core-config.yaml` não tiver nenhum squad registrado com agentes: exibir "Nenhum agente registrado. Adicione squads via `*new-squad` antes de criar workers." e HALT
- [x] A pergunta exibida ao usuário inclui a lista dinâmica como sugestão, mas aceita qualquer resposta — o usuário pode informar um agente não listado (ex: agente em desenvolvimento ainda não registrado no config)
- [x] Nenhuma outra parte da task é alterada

## Fora de Escopo

- Modificar a lógica de agendamento (`*workers {id} schedule`)
- Criar qualquer integração entre workers e stories

## Dependências

- Nenhuma

---

## Execution Log

**Executor:** Claude Code
**Data:** 2026-04-16
**Status anterior → novo:** In Progress → In Review

### O que foi feito

- `kairos-workers.md` Bloco 1, Pergunta 1 reescrita: substituída a lista hardcoded de agentes por instrução de leitura dinâmica de `.kairos-core/core-config.yaml → agents.squads`
- Adicionada lógica de HALT quando nenhum squad com agentes está registrado
- A pergunta mantém tom de sugestão ("Você pode informar qualquer agente"), preservando flexibilidade para agentes ainda não registrados

### Decisões tomadas

- A instrução permanece em prosa (não código) porque `kairos-workers.md` é uma task interpretativa executada pelo @kairos — não um script TypeScript. O @kairos lê o YAML e monta a lista no momento da execução.
- O HALT por ausência de squads está embutido na mesma pergunta (não em um step separado) para manter a coesão do Bloco 1.

### Arquivos criados/modificados

- `.kairos-core/tasks/kairos-workers.md` — Bloco 1, Pergunta 1 atualizada (única mudança)

### Pendências / questões abertas

- Nenhuma

### Notas para @kairos

- Verificar se o formato exato da lista dinâmica (`@{agent-id} (squad: {squad-name})`) está coerente com como o @kairos exibe listas em outros contextos (ex: `*status`)

---

## Change Log

| Data | Evento |
|------|--------|
| 2026-04-15 | Story criada por @kairos |
| 2026-04-16 | Implementação iniciada |
| 2026-04-16 | Implementação concluída — status → In Review |
| 2026-04-16 | *pre-push: gate confirmado — status → Done |
