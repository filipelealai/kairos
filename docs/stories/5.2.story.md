# Story 5.2 — Transição para Repo Público + Squad `ops`

**Epic:** 5 — Arquitetura do Framework
**Status:** In Progress
**Complexidade:** M
**Tipo:** kairos-core
**Criada por:** @kairos
**Data:** 2026-04-16

⚠️  ATENÇÃO — MODIFICAÇÃO DO NÚCLEO DO KAIROS
────────────────────────────────────────────────────────────
Esta story modifica o núcleo do framework Kairos. Alterações
são livres (Kairos é open-source), mas podem impedir futuras
atualizações automáticas, e podem ser sobrescritas por
eventuais atualizações. Ao prosseguir, você estará fazendo
um fork local do Kairos. Continue com consciência — por conta
e risco do usuário.
────────────────────────────────────────────────────────────

## Objetivo

Executar a transição física para repo público: separar `main` (framework puro, `filipelealweb/kairos` público) de `filipe-instance` (branch privada em `filipelealweb/kairos-pessoal`), e criar o squad `ops` com a task `push-dual` que mantém os dois remotes sincronizados com um único comando.

## Contexto

A separação lógica foi concluída na story 5.1 (manifesto, default-deny, relocações). Esta story executa a separação física e cria a infraestrutura de instância para gerenciar o fluxo dual de push.

**Arquitetura completa (local + remotes):**
```
LOCAL — um único repo git, dois branches, dois remotes

  branch filipe-instance  ←── você trabalha aqui (tudo: framework + instância)
         │                       ↓ push-dual
         │                git push private filipe-instance
         │                       ↓
         │                filipelealweb/kairos-pessoal  (GitHub privado)
         │
         └─ sync automático via push-dual (git checkout filipe-instance -- <framework files>)
                ↓
  branch main             ←── nunca tocada diretamente; gerenciada pelo push-dual
                                ↓ push-dual
                          git push origin main
                                ↓
                          filipelealweb/kairos  (GitHub público — só framework)
```

**Remotes:**
```
origin  → filipelealweb/kairos         (público — framework puro)
private → filipelealweb/kairos-pessoal  (privado — conteúdo completo)
```

**Contrato framework/instância:**
- `push-dual.md` vive em `squads/ops/tasks/` — user-owned por design de squad
- `kairos-push.md` (framework L2) permanece inalterado
- O squad `ops` é o namespace canônico para tasks operacionais desta instância

---

## Critérios de Aceite

- [ ] Repo `filipelealweb/kairos-pessoal` criado como privado no GitHub (`gh repo create`)
- [ ] Remote `private` adicionado localmente: `git remote add private <url>`
- [ ] Branch `filipe-instance` criada a partir do `main` atual; `git log --oneline -3` idêntico em ambas
- [ ] `main` limpa: `.gitignore` atualizado com conteúdo de instância + `git rm --cached` executado para todos os paths de instância; `git status` em `main` não mostra arquivos de instância
- [ ] Commit de limpeza em `main` com mensagem clara
- [ ] `filipe-instance` preserva conteúdo completo — `git diff main..filipe-instance` mostra apenas arquivos de instância
- [ ] Squad `ops` scaffoldado em `squads/ops/`: `squad.yaml`, `README.md`, `tasks/push-dual.md`
- [ ] `push-dual.md` implementa: (1) push `filipe-instance` → `private`, (2) `git checkout main` + `git checkout filipe-instance -- {cada path em manifest.owned_files}`, (3) commit + push `main` → `origin`, (4) retorno para `filipe-instance`
- [ ] Squad `ops` adicionado à seção "Squads ativos" do `CLAUDE.md` (user-owned)
- [ ] `*doctor` HEALTHY em `main` limpa (WARNs de SHA drift esperados até story 5.3)

---

## Fora de Escopo

- Refresh de SHAs no manifesto — story 5.3
- Tornar `filipelealweb/kairos` público via `gh repo edit` — pode ser feito manualmente pelo usuário após verificação da limpeza (ou incluído como último passo)
- Configuração de branch protection no repo público
- CLI `npx install kairos` — story 5.4

---

## Notas para o Executor

**Ordem crítica — não inverter:**
1. Criar `filipe-instance` ANTES de modificar `main`
2. Verificar `git diff main..filipe-instance` ANTES de push ao remote público
3. Criar repo privado `kairos-pessoal` ANTES de configurar o remote

**`.gitignore` — conteúdo a adicionar em `main`:**
```gitignore
# Instance-specific content — not shipped as framework
squads/
!squads/.gitkeep

src/agents/*.ts
!src/agents/.gitkeep

.kairos-core/agents/
!.kairos-core/agents/.gitkeep

data/outputs/
!data/outputs/.gitkeep

.claude/settings.local.json

# Squad personas (user-created, not shipped)
.claude/commands/kairos/agents/*.md
!.claude/commands/kairos/agents/kairos.md

# Project metadata (instance-owned)
docs/stories/
docs/epics/
docs/qa/
docs/scope.md
```

**Squad `ops` — é task-based, sem agent persona obrigatória.** O executor decide se cria um `@ops` agent ou deixa o squad apenas com tasks executadas diretamente pelo @kairos. Dado que `push-dual` é uma operação de governança de repo, faz sentido ser executada pelo @kairos sem persona intermediária.

**`push-dual.md` — mecanismo de sync:**
A task lê `manifest.yaml` para obter a lista de `owned_files`, usa `git checkout filipe-instance -- {path}` para cada arquivo, garantindo que `main` recebe exatamente e somente os arquivos framework — sem depender de `.gitignore` para a separação.

---

## Dependências

- Story 5.1 Done ✅
- `docs/public-repo-transition.md` — plano documentado ✅
- `gh` CLI autenticado com permissão para criar repos

---

## Change Log

| Data | Evento |
|------|--------|
| 2026-04-16 | Story criada por @kairos (design revisado: squad ops, remote swap kairos-pessoal) |
| 2026-04-16 | Implementação iniciada |
