# Story 3.7 — scope.md do Framework em .kairos-core/docs/ e *architecture {squad}

**Epic:** 3 — Governança e Versionamento
**Status:** Done
**Complexidade:** M
**Tipo:** kairos-core
**Criada por:** @kairos
**Data:** 2026-04-15

---

## Objetivo

Corrigir a contradição entre a `kairos-prd.md` ("O PRD do Kairos vive em `docs/scope.md`") e o manifest ("docs/scope.md é metadado de instância"): criar `.kairos-core/docs/scope.md` como o documento de escopo do framework (kairos-owned, L3), manter `docs/scope.md` como escopo da instância gerenciado por `*prd`, e expandir `*architecture` para aceitar `{squad}` e gerenciar data-flow docs tanto a nível de framework quanto de squad.

---

## Critérios de Aceite

### Novo arquivo de escopo do framework

- [x] `.kairos-core/docs/scope.md` criado — documento de escopo e arquitetura do Kairos **como framework** (genérico, sem referências a squads específicos desta instância); inclui frontmatter `kairos-owned: true`; estrutura: visão geral, propósito, arquitetura de agentes, modelo de governance, stack de referência
- [x] `.kairos-core/docs/scope.md` adicionado ao `manifest.yaml` como L3
- [x] `docs/scope.md` permanece como escopo **da instância** (user-owned, não entra no manifest — já classificado como metadado de instância nas notas do manifest)

### *prd: clarificado como gestão do escopo da instância

- [x] `kairos-prd.md` atualizado — remover "O PRD do Kairos vive em `docs/scope.md`"; substituir por "docs/scope.md é o escopo desta instância do Kairos — descreve os squads, objetivos e restrições específicos deste projeto"
- [x] Elicitação do `*prd` (Pergunta 1) atualizada: "Para quem esta **instância** do Kairos serve e qual é o foco dela?" (não "o que é o Kairos" — isso é framework)
- [x] Formato de `docs/scope.md` atualizado: título do arquivo passa de "Kairos — Escopo e Objetivos" para "Instância Kairos — Escopo e Objetivos da Instância"
- [x] `core-config.yaml` (chave `framework.scopeFile`) atualizada: `scopeFile: docs/scope.md` → `frameworkScopeFile: .kairos-core/docs/scope.md`; nova chave `instanceScopeFile: docs/scope.md` adicionada

### *architecture: leitura dinâmica e modo squad

- [x] `kairos-architecture.md` — Passo 1 (Stack): substituir leitura de `docs/scope.md` por leitura de `.kairos-core/docs/scope.md` para a stack declarada do framework; `docs/scope.md` (instância) referenciado separadamente como "escopo da instância"
- [x] `kairos-architecture.md` — Passo 4 (Data Flow): remover referências hardcoded a `campaign-analyst.ts` e outros scripts de squad; substituir por leitura dinâmica de `core-config.yaml → agents.squads` para descobrir os agentes ativos e seus scripts em `src/agents/`
- [x] `kairos-architecture.md` — novo modo `*architecture {squad}`:
  - Recebe o nome de um squad como argumento
  - Verifica se `squads/{squad}/` existe; se não, HALT com lista de squads disponíveis
  - Cria ou atualiza `squads/{squad}/workflows/data-flow.md` descrevendo o fluxo de dados do squad: inputs, etapas do pipeline, outputs, integrações externas
  - Valida consistência: agentes declarados em `squad.yaml` têm scripts e estão documentados no data-flow?
  - Resultado: CONSISTENTE / DRIFT / INCOMPLETO
- [x] `kairos-architecture.md` — sem argumento (comportamento atual): mantém auditoria geral do framework, agora lendo dinamicamente os agentes de `core-config.yaml` em vez de hardcodar

### Persona e Help

- [x] `kairos-help.md` — assinatura de `*architecture` atualizada para `*architecture [{squad}]`; descrição expandida para mencionar o modo squad
- [x] Persona `kairos.md` — descrição do comando `*architecture` atualizada: `*architecture [{squad}]`

## Fora de Escopo

- Migrar o conteúdo existente de `docs/scope.md` para `.kairos-core/docs/scope.md` (o conteúdo atual de `docs/scope.md` é da instância — fica onde está)
- Implementar `*architecture all-squads` para rodar o modo squad em todos os squads de uma vez
- Criar `squads/{squad}/workflows/` se não existir (pré-condição esperada do scaffold de *new-squad)

## Dependências

- Nenhuma (independente das outras stories do Epic 3)

---

## Execution Log

**Executor:** Claude Code
**Data:** 2026-04-16
**Status anterior → novo:** In Progress → In Review

### O que foi feito

- Criado `.kairos-core/docs/scope.md` — escopo e arquitetura do Kairos como framework (genérico, sem referências a instâncias específicas); inclui visão geral, propósito, arquitetura de agentes, modelo de governança, stack de referência e estrutura de diretórios
- Adicionado `.kairos-core/docs/scope.md` ao `manifest.yaml` como L3 com SHA placeholder (a ser calculado no *pre-push)
- Atualizado `kairos-prd.md`: texto de introdução corrigido, Pergunta 1 reframeada como "Foco desta instância", formato do título de `docs/scope.md` atualizado para "Instância Kairos — Escopo e Objetivos da Instância"
- Atualizado `docs/scope.md`: título da instância corrigido para "Instância Kairos — Escopo e Objetivos da Instância"
- Atualizado `core-config.yaml`: `scopeFile` renomeado para `frameworkScopeFile` (apontando para `.kairos-core/docs/scope.md`) e adicionado `instanceScopeFile: docs/scope.md`
- Reescrito `kairos-architecture.md`: Passo 1 lê `.kairos-core/docs/scope.md` para stack do framework; Passo 4 usa descoberta dinâmica via `core-config.yaml`; adicionado Modo Squad completo (Passos M1–M5) com formato canônico de data-flow.md
- Atualizado `kairos-help.md`: assinatura `*architecture [{squad}]`, descrição expandida e cheat sheet
- Atualizado `kairos.md` (persona): description do comando `*architecture`, cheat sheet rápido, REQUEST-RESOLUTION e seção Guia

### Decisões tomadas

- **SHA placeholder no manifest:** o SHA de `.kairos-core/docs/scope.md` foi inserido como placeholder (`placeholder-story-3.7`) — o valor real deve ser calculado e atualizado pelo `*pre-push` ou `*doctor`. Alternativa (calcular agora via sha256sum) foi considerada, mas é responsabilidade do fluxo de push garantir integridade.
- **Pergunta 1 do *prd reframeada:** o label "O que é o Kairos?" foi removido pois induz a descrever o framework, não a instância. O novo label "Foco desta instância" é mais direto e correto.
- **`*architecture {squad}` cria `squads/{squad}/workflows/data-flow.md`** — o story exclui explicitamente criar o diretório `workflows/` se não existir (pré-condição do *new-squad). O modo M4 propõe atualização ao usuário antes de sobrescrever arquivo existente.

### Arquivos criados/modificados

- `.kairos-core/docs/scope.md` — criado (escopo do framework, L3, kairos-owned)
- `.kairos-core/manifest.yaml` — adicionada entrada L3 para scope.md
- `.kairos-core/tasks/kairos-prd.md` — texto de introdução, Pergunta 1 e formato de título atualizados
- `.kairos-core/tasks/kairos-architecture.md` — reescrito com leitura dinâmica e modo squad
- `.kairos-core/core-config.yaml` — `scopeFile` → `frameworkScopeFile` + `instanceScopeFile`
- `.kairos-core/tasks/kairos-help.md` — assinatura e descrição de `*architecture` atualizadas
- `.claude/commands/kairos/agents/kairos.md` — command description, cheat sheet e guide atualizados
- `docs/scope.md` — título corrigido para instância
- `docs/stories/3.7.story.md` — ACs marcados, Execution Log adicionado, status → In Review

### Pendências / questões abertas

- SHA de `.kairos-core/docs/scope.md` no manifest está como placeholder — deve ser atualizado com o hash real antes do push

### Notas para @kairos

- Verificar se o SHA placeholder no manifest é aceitável para PASS (é uma prática usada em outras stories) ou se deve ser calculado agora
- O AC sobre `docs/scope.md` permanecer como user-owned está correto: o arquivo continua fora do manifesto conforme as notas do manifest confirmam ("docs/scope.md é metadado da instância")

---

## Change Log

| Data | Evento |
|------|--------|
| 2026-04-15 | Story criada por @kairos |
| 2026-04-16 | Implementação iniciada |
| 2026-04-16 | Implementação concluída — status → In Review |
| 2026-04-16 | *pre-push: gate confirmado — status → Done |
