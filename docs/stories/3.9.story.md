# Story 3.9 — Pre-Push: Prompt Opcional de Bump para Stories PATCH

**Epic:** 3 — Governança e Versionamento
**Status:** Done
**Complexidade:** P
**Tipo:** kairos-core
**Criada por:** @kairos
**Data:** 2026-04-16

---

## Objetivo

O `*pre-push` atual só detecta e solicita bump de versão para stories MINOR e MAJOR. Stories PATCH são silenciosamente ignoradas, forçando o usuário a rodar `*version patch` manualmente caso queira registrar o bump — ou deixar sem bump. Adicionar um prompt opcional não-bloqueante para PATCH: "Story PATCH sem bump desde o último release — deseja bumpar agora? (s/n)".

---

## Critérios de Aceite

- [x] `kairos-pre-push.md` — Passo 2 (versionamento) expandido: após tratar MINOR/MAJOR, verifica se existe story PATCH com gate PASS/RESSALVA sem bump de versão correspondente no `CHANGELOG.md`
- [x] Se story PATCH sem bump detectada: exibe prompt **não-bloqueante**: "Story {id} é PATCH — sem bump desde o último release. Deseja bumpar agora? (s/n)" — se não: continua normalmente; se sim: executa bump PATCH inline e continua
- [x] O prompt não aparece se já houve bump PATCH desde o gate da story (verificação por data: data do gate vs data da entrada mais recente no CHANGELOG.md)
- [x] Stories PATCH sem gate algum (não passaram por *review) não disparam o prompt — o check é condicionado à existência de gate PASS/RESSALVA
- [x] Comportamento para MINOR/MAJOR permanece inalterado (bloqueante, obrigatório)

## Fora de Escopo

- Tornar o bump PATCH obrigatório (continua opcional — PATCH não exige story nem bump)
- Modificar `kairos-version-bump.md` (sem mudança)
- Exibir o prompt para stories sem gate (apenas stories que passaram por *review)

## Dependências

- Story 3.2 Done (`kairos-pre-push.md` já reescrita com estrutura de 5 passos)

---

## Execution Log

**Executor:** Claude Code
**Data:** 2026-04-16
**Status anterior → novo:** In Progress → In Review

### O que foi feito

- Expandido Passo 2 de `kairos-pre-push.md` com novo sub-passo 4: detecta stories PATCH com gate PASS/RESSALVA mas sem bump correspondente no CHANGELOG.md
- Prompt não-bloqueante adicionado: exibe aviso e pergunta ao usuário se deseja bumpar PATCH, sem bloquear o fluxo se o usuário recusar
- Lógica de skip adicionada: se data do CHANGELOG >= data do gate, o prompt não aparece (bump já realizado)
- Condição de guarda explícita: stories PATCH sem gate algum não disparam o prompt
- Resultado Final atualizado para refletir outcome do PATCH no sumário do Passo 2
- `kairos-version` do frontmatter da task atualizado para 3.1.0

### Decisões tomadas

- **Identificação de PATCH por exclusão:** a lógica de identificar se a story é PATCH segue a mesma heurística do Passo 1 (não é MINOR nem MAJOR), mantendo coerência sem introduzir novo mecanismo
- **Posição como step 4 dentro do Passo 2:** inserido após o processamento MINOR/MAJOR existente para não alterar o fluxo bloqueante; o step 4 é completamente autônomo
- **Bump inline reusa passos 2.a–2.e:** referenciado explicitamente para evitar duplicação de lógica na task; mantém sincronia com `kairos-version-bump.md`

### Arquivos criados/modificados

- `.kairos-core/tasks/kairos-pre-push.md` — Passo 2 expandido com step 4 (prompt opcional PATCH); Resultado Final atualizado; kairos-version bumped para 3.1.0

### Pendências / questões abertas

- Nenhuma

### Notas para @kairos

- O step 4 referencia "passos 2.a–2.e acima" — verificar se essa referência está clara o suficiente ou se deve ser renumerada explicitamente
- A identificação de PATCH por exclusão (não MINOR/MAJOR) depende da mesma heurística do Passo 1; se essa heurística evoluir, o step 4 deve evoluir junto

## Change Log

| Data | Evento |
|------|--------|
| 2026-04-16 | Story criada por @kairos |
| 2026-04-16 | Implementação iniciada |
| 2026-04-16 | Implementação concluída — status → In Review |
| 2026-04-16 | *pre-push: gate confirmado — status → Done |
