# Story 3.6 — Executor @kairos para Instâncias: *implement, src/ e Modelo de Autoridade

**Epic:** 3 — Governança e Versionamento
**Status:** Done
**Complexidade:** G
**Tipo:** kairos-core
**Criada por:** @kairos
**Data:** 2026-04-15

---

## Objetivo

Formalizar @kairos como executor de stories `type: instance` (squads, workers, agentes e scripts associados), introduzir o comando `*implement` dedicado a essa execução, clarificar que todo `src/` (tools e agents) é domínio do @kairos — não do Claude Code plain — e atualizar a matriz de autoridade (L1) e o ciclo de vida de stories (L3) para refletir o modelo correto. Stories `type: kairos-core` continuam sendo implementadas por Claude Code plain; @kairos governa, exibe o aviso e revisa.

---

## Contexto Arquitetural

O princípio constitucional 10 diz "@kairos não substitui o trabalho **operacional** dos squads — governa, não executa." "Operacional" = o que os squads fazem em seu domínio (analisar leads, gerar e-mails). Isso nunca impediu @kairos de **implementar a estrutura** dos squads — aliás, `*new-squad` já é EXCLUSIVO de @kairos. A lacuna atual é que a `story-lifecycle.md` e a seção "Quem Constrói o Kairos" em `agent-authority.md` não distinguem entre implementação de framework e implementação de instâncias, tratando Claude Code como executor universal. Esta story corrige essa omissão.

---

## Critérios de Aceite

### Modelo de Autoridade (L1)

- [x] `agent-authority.md` — nova seção adicionada: "Quem Implementa Instâncias":
  - Stories `type: instance` → @kairos é o executor via `*implement`
  - Stories `type: kairos-core` → Claude Code plain é o executor (permanece como hoje)
  - Todo `src/` (tools e agents): gerenciado por @kairos — `src/tools/*.ts` como infraestrutura de instâncias (L2 no manifesto, authority de @kairos), `src/agents/*.ts` como scripts de instância implementados via `*implement`
  - Claude Code plain não escreve arquivos em `src/` sob o novo modelo

### Ciclo de Vida (L3)

- [x] `story-lifecycle.md` — nova seção "Executor por Tipo de Story":
  - `type: instance`: @kairos permanece ativo durante toda a implementação; usa `*implement`; Execution Log assinado "@kairos (via *implement)"
  - `type: kairos-core`: Claude Code plain implementa (sem persona ativa); @kairos governa (exibe aviso por story 3.5), revisa via `*review` e move para Done

### *implement — Nova Task e Comando

- [x] Nova task `.kairos-core/tasks/kairos-implement.md` criada com os seguintes comportamentos:
  - Argumento `{story-id}` opcional: se omitido, lista stories `type: instance` com status Draft ou In Progress e pede ao usuário que escolha
  - Recusa explícita para stories `type: kairos-core`: "Esta story modifica o framework (type: kairos-core) — implementação é por Claude Code plain. Saia do modo @kairos para implementar."
  - Move story Draft → In Progress ao iniciar; registra no Change Log da story
  - Lê Objetivo, ACs e arquivos esperados da story; implementa arquivo por arquivo
  - Ao concluir: adiciona seção `## Execution Log` (executor: "@kairos via *implement"), move para In Review
  - Exibe: "✅ Implementação concluída. Rode `*review {id}` para validar e gerar o gate."
- [x] Gate de `*review` para stories `type: instance` implementadas via `*implement` inclui campo `self_reviewed: true` e nota: "Story implementada e revisada pelo mesmo agente — validação estrutural (ACs, arquivos), não independente."

### Integração com *new-squad e *new-worker

- [x] `kairos-new-squad.md` — ao final da auto-criação de story (vide story 3.5), oferecer: "Story {id} criada em Draft. Implementar o squad agora? (s = *implement {id}, n = fazer depois)"
  - Se s: executar `*implement {id}` inline na mesma sessão
- [x] `kairos-workers.md` — se o worker criado via `*workers new` gerar uma story associada (tipo: instance), oferecer a mesma sugestão de `*implement` ao final

### Persona e Help

- [x] Persona `kairos.md` — comando `*implement [{story-id}]` adicionado:
  - visibility: [full, quick, key]
  - description: "Implementar story type: instance — move Draft→InReview, executa ACs, adiciona Execution Log"
  - Recusa para type: kairos-core
- [x] `kairos-help.md` — `*implement` adicionado na Seção 2 (comandos); Fluxo de instâncias (④ e ⑤) atualizado: `*new-squad` → story criada → `*implement` → `*review` → `*pre-push` → `*push`; Seção "Quando NÃO usar @kairos" revisada (antes dizia que @kairos não executa; agora: @kairos não faz trabalho operacional dos squads, mas implementa instâncias)

### Versão

- [x] Esta story requer bump **MAJOR** (2.3.0 → 3.0.0): mudança em `agent-authority.md` (L1) — adição de seção que estende o modelo de autoridade
- [x] Justificativa registrada no CHANGELOG: "Extensão do modelo de autoridade: @kairos formalizado como executor de stories type: instance; Claude Code plain reservado para type: kairos-core; todo src/ sob domínio @kairos"

## Fora de Escopo

- Modificar `*review` além do campo `self_reviewed` para type: instance
- Implementar mecanismo de atualização automática do Kairos (Epic 5)
- Modificar a constituição — o princípio 10 já suporta este modelo com a leitura correta ("operacional" ≠ "implementação de estrutura")
- Criar executor independente para type: kairos-core (permanece Claude Code plain)

## Dependências

- Story 3.5 Done (`type: instance` field existe antes de `*implement` referenciar o tipo)
- Story 3.3 Done (fluxos e help base atualizados antes de adicionar *implement)

---

## Execution Log

**Executor:** Claude Code
**Data:** 2026-04-16
**Status anterior → novo:** In Progress → In Review

### O que foi feito

- `agent-authority.md` (L1): nova seção "Quem Implementa Instâncias" adicionada com tabela executor por tipo, regras para `src/`, e revisão de "Quem Constrói o Kairos" para refletir os dois executores. `kairos-version` bumped para 3.0.0.
- `story-lifecycle.md` (L3): nova seção "Executor por Tipo de Story" adicionada no topo do arquivo, com tabela e regras dos dois fluxos. Seção "Quem é o Executor" renomeada para "Quem é o Executor (kairos-core)" para manter clareza. `kairos-version` bumped para 3.0.0.
- `.kairos-core/tasks/kairos-implement.md`: nova task criada com 7 passos — resolve story_id, verifica tipo (recusa kairos-core com mensagem explícita), move Draft→In Progress, lê story, implementa AC por AC, adiciona Execution Log assinado "@kairos via *implement", move para In Review. Inclui seção especial para gate `self_reviewed` em `*review`.
- `kairos-new-squad.md`: seção "Notas pós-scaffolding" substituída por oferta interativa de `*implement` inline com fluxo s/n.
- `kairos-workers.md`: passo de confirmação do `*workers new` expandido para detectar se o worker precisa de implementação e oferecer `*implement` inline quando story `type: instance` for criada.
- `kairos.md` (persona): comando `*implement [{story-id}]` adicionado com visibility [full, quick, key]; 3 entradas de REQUEST-RESOLUTION adicionadas; `kairos-implement.md` adicionado em dependencies.tasks; Comandos Rápidos e Guia (*guide) atualizados com dois ciclos distintos (kairos-core vs instance).
- `kairos-help.md`: `*implement` adicionado na Seção 2; fluxos ④ e ⑤ atualizados; Seção 4 reescrita distinguindo trabalho operacional (squads) de implementação de instâncias (@kairos); Seção 5 com regra sobre os dois executores; cheat sheet atualizado; `*help stories` revisado com executor por tipo e assinatura do Execution Log.
- `core-config.yaml`: version 2.3.0 → 3.0.0.
- `CHANGELOG.md`: entrada [3.0.0] criada com justificativa completa de MAJOR.

### Decisões tomadas

- **Bump MAJOR de 2.3.0 (não 2.0.0 como indicado na story):** a story mencionava "2.0.0 → 3.0.0" na justificativa, mas a versão corrente ao iniciar era 2.3.0. O bump correto é 2.3.0 → 3.0.0 (MAJOR a partir da versão atual, não da original da story).
- **`self_reviewed` na task `kairos-implement.md` (não em `kairos-review.md`):** a instrução de adicionar o campo foi colocada na task de implement como instrução para o `*review` posterior — evita modificar `kairos-review.md` além do que a story pede (fora de escopo).
- **Seção "Quem é o Executor" renomeada para "(kairos-core)":** mantém continuidade semântica com o texto existente sem duplicar conteúdo — a nova seção "Executor por Tipo de Story" cobre ambos os tipos, a seção renomeada detalha apenas o fluxo kairos-core.

### Arquivos criados/modificados

- `.claude/rules/agent-authority.md` — modificado (nova seção + revisão de seção existente)
- `.claude/rules/story-lifecycle.md` — modificado (nova seção no topo)
- `.kairos-core/tasks/kairos-implement.md` — criado
- `.kairos-core/tasks/kairos-new-squad.md` — modificado (seção final substituída)
- `.kairos-core/tasks/kairos-workers.md` — modificado (passo 7 expandido)
- `.claude/commands/kairos/agents/kairos.md` — modificado (comando, REQUEST-RESOLUTION, dependencies, Rápidos, Guia)
- `.kairos-core/tasks/kairos-help.md` — modificado (Seção 2, 3, 4, 5, cheat sheet, *help stories)
- `.kairos-core/core-config.yaml` — modificado (version 2.3.0 → 3.0.0)
- `CHANGELOG.md` — modificado (entrada [3.0.0] adicionada)

### Pendências / questões abertas

- Nenhuma

### Notas para @kairos

- Verificar se a instrução de `self_reviewed` em `kairos-implement.md` (como nota para o `*review`) é suficiente ou se `kairos-review.md` precisa de atualização explícita para detectar e incluir o campo automaticamente.
- O bump foi de 2.3.0 → 3.0.0 (não 2.0.0 → 3.0.0 como indicado no AC) — o AC provavelmente refletia a versão vigente quando a story foi planejada.

---

## Change Log

| Data | Evento |
|------|--------|
| 2026-04-15 | Story criada por @kairos |
| 2026-04-16 | Implementação iniciada |
| 2026-04-16 | Implementação concluída — status → In Review |
| 2026-04-16 | *pre-push: gate confirmado — status → Done |
