# Story 3.11 — Renomear `*review-squad` → `*validate-squad`

**Epic:** 3
**Status:** Done
**Complexidade:** P
**Tipo:** kairos-core
**Criada por:** @kairos
**Data:** 2026-04-16

---

## Objetivo

Renomear o comando `*review-squad` para `*validate-squad` em todos os artefatos do framework, alinhando a nomenclatura com o padrão já estabelecido por `*validate-story`.

`*review-squad` e `*validate-story` têm natureza idêntica: ambos validam um **artefato estático** (squad config vs. documento de story) verificando coerência e completude, sem tocar em implementação e sem gerar gate file. O prefixo `review` cria falsa analogia com `*review` (que é pesado: valida implementação, gera gate em `docs/qa/gates/`, emite PASS/RESSALVA/BLOCK). O prefixo `validate` é semanticamente correto e consistente.

---

## Critérios de Aceite

- [x] Arquivo `.kairos-core/tasks/kairos-review-squad.md` renomeado para `kairos-validate-squad.md`
- [x] Campo `command` dentro do arquivo da task atualizado de `*review-squad {squad}` para `*validate-squad {squad}`
- [x] Persona do `@kairos` (`.claude/commands/kairos/agents/kairos.md`) atualizada: entrada do comando renomeada de `review-squad` para `validate-squad` em todos os blocos (`commands`, `dependencies.tasks`, `REQUEST-RESOLUTION`, seção de comandos rápidos)
- [x] Nenhuma referência a `*review-squad` ou `kairos-review-squad` permanece em arquivos de framework (`.kairos-core/`, `.claude/`, `docs/`)
- [x] CHANGELOG.md tem entrada descrevendo o rename (PATCH)
- [x] Versão bumped para PATCH em `.kairos-core/core-config.yaml`

---

## Fora de Escopo

- Modificar o comportamento da task — apenas rename, sem alteração funcional
- Atualizar READMEs, documentação de squads ou MEMORY.md dos agentes (não referenciam o comando diretamente)

---

## Dependências

- Nenhuma story predecessora obrigatória — change isolado e seguro

---

## Execution Log

**Executor:** Claude Code
**Data:** 2026-04-17
**Status anterior → novo:** In Progress → In Review

### O que foi feito

- `kairos-review-squad.md` removido e `kairos-validate-squad.md` criado com conteúdo atualizado (id, command, title, verdict box, exemplo de output)
- `kairos.md` atualizado: `REQUEST-RESOLUTION` (3 mapeamentos), `commands` (name + task), `dependencies.tasks`, seção "Comandos Rápidos"
- `kairos-help.md` atualizado: Seção 2 (descrição + auto-referência interna), Seção 4 (diagnóstico), cheat sheet (2 linhas)
- `CHANGELOG.md`: nova entrada `[3.1.3]` com descrição do rename
- `core-config.yaml`: version `3.1.2` → `3.1.3`, updatedAt atualizado

### Decisões tomadas

- Registros históricos (`docs/stories/3.4.story.md`, `docs/qa/gates/3.4-*.yaml`, entradas antigas do CHANGELOG) mantidos intactos — alterar esses artefatos seria revisão de história, não renomeação de comando. Os arquivos operacionais (`.kairos-core/`, `.claude/`) têm zero referências ao nome antigo.

### Arquivos criados/modificados

- `.kairos-core/tasks/kairos-validate-squad.md` — criado (substitui kairos-review-squad.md)
- `.kairos-core/tasks/kairos-review-squad.md` — removido
- `.claude/commands/kairos/agents/kairos.md` — 4 ocorrências atualizadas
- `.kairos-core/tasks/kairos-help.md` — 4 ocorrências atualizadas
- `CHANGELOG.md` — entrada 3.1.3 adicionada
- `.kairos-core/core-config.yaml` — versão bumped para 3.1.3

### Pendências / questões abertas

- Nenhuma

### Notas para @kairos

- `kairos-review-squad.md` não estava listado no manifest.yaml — `kairos-validate-squad.md` também não foi adicionado. Considerar adicionar ao manifest em story futura para completar o inventário de tasks.

---

## Change Log

| Data | Evento |
|------|--------|
| 2026-04-16 | Story criada por @kairos |
| 2026-04-17 | Implementação iniciada |
| 2026-04-17 | Implementação concluída — status → In Review |
| 2026-04-17 | *pre-push: gate confirmado — status → Done |
