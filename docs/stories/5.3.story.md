# Story 5.3 — SHA Sync Automático no `*pre-push` e `*version`

**Epic:** 5 — Arquitetura do Framework
**Status:** Done
**Complexidade:** P
**Tipo:** kairos-core
**Criada por:** @kairos
**Data:** 2026-04-16

⚠️  ATENÇÃO — MODIFICAÇÃO DO NÚCLEO DO KAIROS
────────────────────────────────────────────────────────────
Esta story modifica o núcleo do framework Kairos. Alterações
são livres (Kairos é open-source), mas podem impedir futuras
atualizações automáticas, e podem ser sobrescritas por
eventuais atualizações. Ao prosseguir, você estará fazendo
um fork local do Kairos. Continue com consciência — por conta
e risco do usuário.
────────────────────────────────────────────────────────────

## Objetivo

Incorporar SHA sync do `manifest.yaml` como parte natural do processo de `*pre-push` e `*version`, eliminando o drift acumulado desde v2.0.0 e garantindo que o manifesto se mantenha atual como efeito colateral automático dos fluxos já existentes.

## Contexto

O `manifest.yaml` foi criado na story 5.1 com SHAs calculados em v2.0.0. O framework evoluiu para v3.1.1 e `*doctor` reporta SHA drift em praticamente todos os `owned_files`. Os SHAs representam o estado canônico de cada arquivo framework na versão vigente — precisam ser mantidos atuais.

**Princípio:** nenhum comando novo, nenhum passo extra para o usuário. SHA sync é uma responsabilidade que cabe naturalmente a `*pre-push` (que já faz versioning como parte do seu processo) e a `*version` (bump manual explícito).

---

## Critérios de Aceite

- [x] `kairos-pre-push.md` atualizado: inclui passo de SHA sync — para cada `owned_files[*]` em `manifest.yaml`, calcula SHA atual do arquivo e atualiza o campo `sha256` se divergir; executa antes do commit final do pre-push
- [x] `kairos-version-bump.md` atualizado: inclui SHA sync como parte do processo de bump — mesmo mecanismo, executado após atualizar `version` e antes de fechar
- [x] One-time refresh executado: todos os SHAs em `manifest.yaml` atualizados para o estado atual (v3.1.2)
- [x] `*doctor` Check 9 retorna zero WARNs de SHA drift após one-time refresh
- [x] `manifest.yaml` commitado com SHAs atualizados como parte desta story

---

## Fora de Escopo

- Mudança na estrutura do manifesto
- Novos campos no manifesto além de `sha256`
- Validação de SHA em outros comandos além de `*pre-push` e `*version`

---

## Dependências

- Story 5.1 Done ✅ — `manifest.yaml` existe
- Story 5.2 Draft — SHA sync em `*pre-push` é particularmente relevante no contexto do `push-dual`, mas não bloqueia esta story

---

## Execution Log

**Executor:** Claude Code
**Data:** 2026-04-17
**Status anterior → novo:** In Progress → In Review

### O que foi feito

- `kairos-pre-push.md`: adicionado Passo 2.6 — SHA Sync entre Passo 2.5 e Passo 3; atualizado checklist do frontmatter e sumário final de resultado
- `kairos-version-bump.md`: adicionada seção "SHA Sync" antes de Confirmação; atualizado checklist do frontmatter e template de confirmação
- One-time refresh: calculados SHAs atuais de todos os 37 `owned_files`; atualizados 14 arquivos com drift; `kairos_version` e `generated_at` do manifesto atualizados para v3.1.2/2026-04-17
- Verificação final: zero drift em todos os `owned_files`

### Decisões tomadas

- **SHA do manifesto após modificar pre-push e version-bump**: os dois arquivos de task foram modificados primeiro, SHAs finais calculados após as edições; o manifesto foi atualizado com os SHAs dos estados finais — garante consistência one-shot sem segunda rodada de sync.
- **Sem novo passo extra no pre-push**: o Passo 2.6 foi inserido entre 2.5 e 3, mantendo o fluxo natural: story Done → SHA sync → commit (o commit já inclui o manifesto atualizado).

### Arquivos criados/modificados

- `.kairos-core/tasks/kairos-pre-push.md` — adicionado Passo 2.6 e entrada no checklist/resultado
- `.kairos-core/tasks/kairos-version-bump.md` — adicionada seção SHA Sync e checklist
- `.kairos-core/manifest.yaml` — 14 SHAs corrigidos; `kairos_version` → 3.1.2; `generated_at` → 2026-04-17
- `docs/stories/5.3.story.md` — status → In Review, ACs marcados, Execution Log adicionado

### Pendências / questões abertas

- Nenhuma

### Notas para @kairos

- O manifesto agora contém SHAs corretos para v3.1.2; próximos bumps via `*version` ou `*pre-push` manterão automaticamente.
- O Passo 2.6 depende de `sha256sum` (padrão Linux/macOS) — verificar se há instâncias em Windows sem WSL.

## Change Log

| Data | Evento |
|------|--------|
| 2026-04-16 | Story criada por @kairos |
| 2026-04-17 | *pre-push: gate confirmado — status → Done |
| 2026-04-17 | Implementação iniciada — status → In Progress |
| 2026-04-17 | Implementação concluída — status → In Review |
