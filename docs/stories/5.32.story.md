# Story 5.32 — Squad-scaffolding Dinâmico — Gerar Personas a partir de `squads/{squad}/agents/*.yaml`

**Epic:** 5
**Status:** Draft
**Complexidade:** G
**Tipo:** kairos-core
**Criada por:** @kairos
**Data:** 2026-04-26

---

⚠️  ATENÇÃO — MODIFICAÇÃO DO NÚCLEO DO KAIROS
────────────────────────────────────────────────────────────
Esta story modifica o núcleo do framework Kairos. Alterações
são livres (Kairos é open-source), mas podem impedir futuras
atualizações automáticas, e podem ser sobrescritas por
eventuais atualizações. Ao prosseguir, você estará fazendo
um fork local do Kairos. Continue com consciência — por conta
e risco do usuário.
────────────────────────────────────────────────────────────

## Objetivo

Tornar o scaffolding de agentes data-driven: a definição em `squads/{squad}/agents/{id}.yaml` (fonte de verdade) gera automaticamente a persona em `.claude/commands/kairos/agents/{id}.md` durante o `*new-squad`, eliminando a criação manual de personas como AC da story de implementação.

## Contexto

Hoje `*new-squad` cria `squads/{squad}/agents/{id}.md` (lightweight — apenas YAML frontmatter com referência) mas não cria a persona em `.claude/commands/kairos/agents/`. Essa criação fica como AC manual na story de implementação gerada pelo scaffolding. A consequência é que, após `*new-squad`, o agente não é ativável até a story de implementação ser executada.

A proposta é tornar o formato de definição canônico (`.yaml`) rico o suficiente para gerar uma persona funcional durante o scaffolding, tornando o agente imediatamente ativável mesmo antes da story de implementação adicionar comportamentos específicos de domínio.

## Critérios de Aceite

- [ ] `.kairos-core/templates/squad-template/agents/agent-id.yaml` criado com schema canônico rico (id, name, icon, whenToUse, persona_profile com tone/greeting/signature_closing, commands com visibility e description, outputs, handoffs)
- [ ] `kairos-new-squad.md` inclui passo de geração da persona em `.claude/commands/kairos/agents/{id}.md` a partir de `squads/{squad}/agents/{id}.yaml`, usando Markdown template embutido na task
- [ ] Persona gerada é funcionalmente completa: contém `activation-instructions`, greeting derivado do `persona_profile`, lista de comandos, identidade e `signature_closing`
- [ ] Agentes existentes do squad `cold-prospecting` migrados de `.md` para `.yaml` em `squads/cold-prospecting/agents/` com schema novo
- [ ] `kairos-doctor.md` inclui check de drift: `squads/*/agents/*.yaml` sem persona correspondente em `.claude/commands/kairos/agents/` → WARNING
- [ ] `kairos-new-squad.md` não inclui mais o AC "criar persona em `.claude/commands/`" nas stories de implementação geradas (persona já existe após scaffolding)

## Fora de Escopo

- Regeneração automática de personas quando o YAML é editado depois do scaffolding (update incremental — story futura)
- CLI externo (`npx kairos generate`) para geração de personas fora do Claude Code
- Migração de agentes de outros squads além de `cold-prospecting`

## Dependências

- Nenhuma bloqueante (5.29 e 5.30 complementares, não bloqueantes)

## Change Log

| Data | Evento |
|------|--------|
| 2026-04-26 | Story criada por @kairos |
